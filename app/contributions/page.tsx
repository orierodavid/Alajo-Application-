'use client'

import { useEffect, useMemo, useState } from 'react'
import Link from 'next/link'
import { AlajoIcon } from '@/components/ui/alajo-icon'

type Row={id:string;periodNumber:number|null;amount:number;dueDate:string;status:string;group:{name:string}|null}
type Summary={total:number;thisMonth:number;upcoming:number;completed:number;missed:number;progress:number}
const money=(n:number)=>`₦${Number(n||0).toLocaleString('en-NG',{minimumFractionDigits:2})}`
const dateText=(v:string)=>new Date(v).toLocaleDateString('en-NG',{day:'numeric',month:'short',year:'numeric'})
const nav=[['dashboard','/dashboard','Dashboard'],['groups','/groups','Groups'],['contributions','/contributions','Contributions'],['payouts','/payouts','Payouts'],['wallet','/wallet','Wallet'],['transactions','/transactions','Transactions'],['invite','/invite-earn','Invite & Earn'],['notifications','/notifications','Notifications'],['settings','/settings','Settings'],['help','/help-center','Help Center'],['logout','/login','Logout']] as const
const paid=['paid'],missed=['overdue','missed','late','defaulted'],upcoming=['pending','processing','due','grace']

export default function ContributionsPage(){
 const[rows,setRows]=useState<Row[]>([]),[summary,setSummary]=useState<Summary>({total:0,thisMonth:0,upcoming:0,completed:0,missed:0,progress:0}),[loading,setLoading]=useState(true),[error,setError]=useState(''),[tab,setTab]=useState<'all'|'completed'|'upcoming'|'missed'>('all'),[name,setName]=useState('User')
 useEffect(()=>{let dead=false;async function load(){try{const r=await fetch('/api/contributions',{cache:'no-store'});const d=await r.json();if(r.status===401||d?.authenticated===false){location.href='/login';return}if(!r.ok||d?.error)throw new Error(d?.error||'Unable to load contributions');if(!dead){setRows(d.rows||[]);setSummary(d.summary||{})}}catch(e){if(!dead)setError(e instanceof Error?e.message:'Unable to load contributions.')}finally{if(!dead)setLoading(false)}}load();fetch('/api/auth/session',{cache:'no-store'}).then(async r=>r.ok?r.json():null).then(d=>{if(!dead&&d?.name)setName(d.name)}).catch(()=>{});return()=>{dead=true}},[])
 const visible=useMemo(()=>tab==='completed'?rows.filter(r=>paid.includes(r.status)):tab==='upcoming'?rows.filter(r=>upcoming.includes(r.status)):tab==='missed'?rows.filter(r=>missed.includes(r.status)):rows,[rows,tab])
 const next=rows.filter(r=>upcoming.includes(r.status)&&new Date(r.dueDate)>=new Date(new Date().setHours(0,0,0,0))).sort((a,b)=>+new Date(a.dueDate)-+new Date(b.dueDate))[0]
 const label=(s:string)=>paid.includes(s)?'Completed':missed.includes(s)?(s==='defaulted'?'Defaulted':'Missed'):s==='processing'?'Processing':s==='grace'?'Grace Period':s==='due'?'Due':'Upcoming'
 const cls=(s:string)=>paid.includes(s)?'bg-green-50 text-[#16a34a]':missed.includes(s)?'bg-red-50 text-red-500':'bg-blue-50 text-blue-500'
 return <div className="min-h-screen bg-[#f8faf9] text-gray-900 flex"><aside className="hidden lg:flex w-[250px] shrink-0 min-h-screen bg-[#0b2313] text-white p-5 flex-col fixed inset-y-0 left-0"><Link href="/dashboard" className="px-2 text-[26px] font-extrabold">Alajo</Link><nav className="mt-8 flex-1 space-y-1 text-[14px]">{nav.map(([icon,href,label])=><Link key={label} href={href} className={`flex items-center gap-3 px-3 py-2.5 rounded-lg ${label==='Contributions'?'bg-white/10 text-white':'text-gray-300 hover:bg-white/5'}`}><AlajoIcon name={icon} size={18}/><span>{label}</span></Link>)}</nav><div className="bg-[#123524] rounded-xl p-4"><p className="font-semibold text-[14px]">Stay consistent, grow together</p><p className="text-[12px] text-gray-300 mt-1">Make your contributions on time and reach your goals faster.</p></div></aside><main className="lg:ml-[250px] flex-1 min-w-0"><header className="h-[76px] bg-white border-b border-gray-100 px-5 sm:px-8 flex items-center justify-between"><div><p className="text-gray-400 text-[13px]">Contributions</p><h1 className="font-semibold text-[18px]">My Contributions</h1></div><div className="flex items-center gap-4"><AlajoIcon name="notifications" size={19}/><div className="w-9 h-9 rounded-full bg-gray-200 flex items-center justify-center font-semibold">{name.charAt(0).toUpperCase()}</div></div></header><section className="p-5 sm:p-8 max-w-[1250px]"><p className="text-gray-500 text-[14px]">Track the contribution schedules assigned to your savings groups.</p>{error&&<div className="mt-5 rounded-xl bg-red-50 text-red-700 p-4 text-sm">{error}</div>}{loading?<div className="mt-6 bg-white rounded-xl border border-gray-100 p-10 text-center text-gray-500">Loading contributions…</div>:<><div className="mt-6 grid grid-cols-2 xl:grid-cols-4 gap-5"><Stat title="Total Contributions" value={money(summary.total)} sub="Assigned schedules" icon="wallet"/><Stat title="This Month" value={money(summary.thisMonth)} sub={new Date().toLocaleDateString('en-NG',{month:'long',year:'numeric'})} icon="clock"/><Stat title="Upcoming" value={money(summary.upcoming)} sub="Due in the next 7 days" icon="payouts"/><Stat title="Completed" value={money(summary.completed)} sub="Paid schedules" icon="check"/></div><div className="mt-6 grid grid-cols-1 xl:grid-cols-3 gap-5"><div id="contributions" className="xl:col-span-2 bg-white rounded-xl border border-gray-100 p-5"><div className="flex gap-6 text-[14px] font-medium border-b border-gray-100 pb-3 overflow-x-auto">{([['all','All Contributions'],['completed','Completed'],['upcoming','Upcoming'],['missed','Missed']] as const).map(([k,v])=><button key={k} onClick={()=>setTab(k)} className={tab===k?'text-[#16a34a] border-b-2 border-[#16a34a] pb-3 -mb-3 whitespace-nowrap':'text-gray-400 whitespace-nowrap pb-3 -mb-3'}>{v}</button>)}</div><div className="mt-4">{visible.length?visible.map(r=><div key={r.id} className="flex items-center justify-between gap-4 py-3 border-b border-gray-50 last:border-0"><div className="flex items-center gap-3 min-w-0"><div className={`w-9 h-9 rounded-full flex items-center justify-center shrink-0 ${cls(r.status)}`}><AlajoIcon name={paid.includes(r.status)?'check':missed.includes(r.status)?'info':'clock'} size={17}/></div><div className="min-w-0"><p className="font-medium truncate">{money(r.amount)} Contribution</p><p className="text-gray-400 text-[12px]">{r.group?.name||'Savings Group'}{r.periodNumber?` • Period ${r.periodNumber}`:''}<br/>Due {dateText(r.dueDate)}</p></div></div><span className={`font-medium text-[12px] rounded px-2 py-0.5 shrink-0 ${cls(r.status)}`}>{label(r.status)}</span><span className="font-semibold shrink-0">{money(r.amount)}</span></div>):<div className="py-12 text-center"><p className="font-medium text-gray-700">No contributions in this view</p><p className="text-sm text-gray-400 mt-1">Admin-created contribution schedules will appear here when assigned to your memberships.</p></div>}</div></div><div className="space-y-5"><div className="bg-white rounded-xl border border-gray-100 p-5"><h3 className="font-semibold">Contribution Summary</h3><div className="mt-4 flex items-center gap-4"><div className="relative w-20 h-20"><svg viewBox="0 0 36 36" className="w-20 h-20 -rotate-90"><circle cx="18" cy="18" r="16" fill="none" stroke="#e5e7eb" strokeWidth="3"/><circle cx="18" cy="18" r="16" fill="none" stroke="#16a34a" strokeWidth="3" strokeDasharray={`${summary.progress},100`}/></svg><span className="absolute inset-0 flex items-center justify-center text-[16px] font-bold">{summary.progress}%</span></div><div className="text-[13px] space-y-1"><p><span className="text-[#16a34a]">●</span> Completed <b>{money(summary.completed)}</b></p><p><span className="text-blue-500">●</span> Upcoming <b>{money(summary.upcoming)}</b></p><p><span className="text-red-500">●</span> Missed <b>{money(summary.missed)}</b></p></div></div></div><div className="bg-white rounded-xl border border-gray-100 p-5"><h3 className="font-semibold">Next Contribution</h3>{next?<><div className="mt-3"><p className="font-semibold text-[14px]">{next.group?.name||'Savings Group'}</p><p className="text-[12px] text-gray-400 mt-1">{money(next.amount)} • Due {dateText(next.dueDate)}</p></div><Link href={`/contributions/make?id=${next.id}`} className="mt-4 block text-center bg-blue-500 text-white font-semibold text-[14px] py-2.5 rounded-md">Make Contribution</Link></>:<p className="mt-3 text-sm text-gray-400">No upcoming contribution.</p>}</div><div className="bg-white rounded-xl border border-gray-100 p-5"><h3 className="font-semibold">Quick Actions</h3><div className="mt-3 space-y-3 text-[14px] text-gray-700"><Link href="/transactions" className="flex justify-between">Contribution History <span>›</span></Link><Link href="/wallet" className="flex justify-between">Payment Methods <span>›</span></Link><Link href="/settings" className="flex justify-between">Auto Contribution <span>›</span></Link></div></div></div></div></>}</section></main></div>
}
function Stat({icon,title,value,sub}:{icon:'wallet'|'clock'|'payouts'|'check';title:string;value:string;sub:string}){return <div className="bg-white rounded-xl border border-gray-100 p-5"><div className="w-9 h-9 rounded-full bg-green-50 flex items-center justify-center text-[#16a34a]"><AlajoIcon name={icon} size={18}/></div><p className="mt-3 text-gray-500 text-[13px]">{title}</p><p className="text-[20px] font-bold">{value}</p><p className="text-[12px] text-gray-400 mt-1">{sub}</p></div>}
