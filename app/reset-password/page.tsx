'use client'

import Link from 'next/link'
import { FormEvent, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

export default function ResetPasswordPage(){const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');if(password.length<8){setError('Password must be at least 8 characters');return}if(password!==confirm){setError('Passwords do not match');return}setLoading(true);const supabase=createClient();const {error}=await supabase.auth.updateUser({password});if(error)setError(error.message);else{setDone(true);await supabase.auth.signOut()}setLoading(false)}return <main className="min-h-screen grid lg:grid-cols-2"><section className="hidden lg:block p-14 border-r border-gray-100"><Link href="/" className="text-[22px] font-extrabold">Alajo</Link><h1 className="mt-12 text-3xl font-bold max-w-sm">Smart Rotational Savings for <span className="text-[#16a34a]">Everyone</span></h1></section><section className="p-8 lg:p-14 flex items-center"><div className="max-w-[380px] w-full mx-auto text-center"><div className="w-16 h-16 mx-auto rounded-full bg-green-50 flex items-center justify-center text-3xl">🔒</div><h2 className="mt-5 text-2xl font-extrabold">Reset Your Password</h2>{done?<><p className="mt-2 text-gray-500">Your password has been changed successfully.</p><Link href="/login" className="inline-block mt-6 px-6 py-3 rounded-md bg-[#14532d] text-white font-semibold">Back to Login</Link></>:<form onSubmit={submit} className="mt-6 space-y-5 text-left"><label className="block text-sm font-semibold">New Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-1.5 w-full border border-gray-200 rounded-md px-3 py-3 outline-none" /></label><label className="block text-sm font-semibold">Confirm New Password<input required type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="mt-1.5 w-full border border-gray-200 rounded-md px-3 py-3 outline-none" /></label>{error&&<p className="text-sm text-red-600 bg-red-50 p-3 rounded-md">{error}</p>}<button disabled={loading} className="w-full py-3 rounded-md bg-[#14532d] text-white font-semibold">{loading?'Updating…':'Reset Password'}</button></form>}</div></section></main>}
