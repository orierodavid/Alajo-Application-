'use client'

import Link from 'next/link'
import { FormEvent, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

export default function ForgotPasswordPage(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setLoading(true);const supabase=createClient();const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});if(error)setError(error.message);else setSent(true);setLoading(false)}return <main className="min-h-screen grid lg:grid-cols-2"><section className="hidden lg:block p-14 border-r border-gray-100"><Link href="/" className="text-[22px] font-extrabold">Alajo</Link><h1 className="mt-12 text-3xl font-bold max-w-sm">Smart Rotational Savings for <span className="text-[#16a34a]">Everyone</span></h1><p className="mt-3 text-gray-500 max-w-sm">Securely manage your savings and grow together.</p></section><section className="p-8 lg:p-14 flex items-center"><div className="max-w-[380px] w-full mx-auto text-center"><div className="w-16 h-16 mx-auto rounded-full bg-green-50 flex items-center justify-center text-3xl">🔑</div><h2 className="mt-5 text-2xl font-extrabold">Forgot Password?</h2><p className="mt-2 text-gray-500">Enter your email address and we'll send you a link to reset your password.</p>{sent?<div className="mt-6 bg-green-50 rounded-md p-4 text-sm text-gray-700">Reset link sent. Check your email and follow the link to continue.</div>:<form onSubmit={submit} className="mt-6 space-y-5 text-left"><label className="block text-sm font-semibold">Email Address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-1.5 w-full border border-gray-200 rounded-md px-3 py-3 outline-none" /></label>{error&&<p className="text-sm text-red-600 bg-red-50 p-3 rounded-md">{error}</p>}<button disabled={loading} className="w-full py-3 rounded-md bg-[#14532d] text-white font-semibold">{loading?'Sending…':'Send Reset Link'}</button></form>}<Link href="/login" className="block mt-5 text-[#16a34a] text-sm font-medium">← Back to Login</Link></div></section></main>}
