import { createClient } from '@/lib/supabase/server'
export const dynamic='force-dynamic'
type Profile={id:string;full_name:string|null;email:string|null;phone:string|null}
type Kyc={user_id:string;status:string|null;verification_type:string|null;provider_customer_ref:string|null;verified_at:string|null}
type Customer={user_id:string;status:string|null;provider_customer_id:string|null;provider_customer_code:string|null}
const fmt=(v:string|null)=>v?new Intl.DateTimeFormat('en-NG',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v)):'—'
const pretty=(v:string)=>v.replaceAll('_',' ').toLowerCase()
export default async function AdminKycPage(){const supabase=await createClient();const [{data:profiles},{data:kycs},{data:customers}]=await Promise.all([supabase.from('profiles').select('id,full_name,email,phone').order('full_name'),supabase.from('user_kyc_profiles').select('user_id,status,verification_type,provider_customer_ref,verified_at'),supabase.from('provider_customers').select('user_id,status,provider_customer_id,provider_customer_code')]);const kycMap=new Map((kycs??[]).map((x:any)=>[x.user_id,x as Kyc]));const customerMap=new Map((customers??[]).map((x:any)=>[x.user_id,x as Customer]));const rows=((profiles??[]) as Profile[]).map(u=>{const k=kycMap.get(u.id),c=customerMap.get(u.id);const status=String(k?.status??c?.status??'NOT_STARTED').toUpperCase();return{u,k,c,status,verified:status==='VERIFIED'}});const verified=rows.filter(x=>x.verified).length;return <section className="p-5 sm:p-8 max-w-7xl mx-auto text-white"><p className="text-xs font-bold tracking-[.2em] text-[#39c66b]">COMPLIANCE</p><h1 className="text-3xl font-bold mt-2">KYC</h1><p className="text-[#b7c7be] mt-2">Only live Paystack-backed records determine verification. Mock and legacy BVN records are excluded.</p><div className="mt-8 grid grid-cols-1 sm:grid-cols-3 gap-4"><div className="rounded-2xl border border-[#244332] bg-[#0d1d13] p-5"><p className="text-xs text-[#9fb3a8]">All users</p><p className="mt-2 text-2xl font-bold">{rows.length}</p></div><div className="rounded-2xl border border-[#244332] bg-[#0d1d13] p-5"><p className="text-xs text-[#9fb3a8]">Paystack verified</p><p className="mt-2 text-2xl font-bold text-[#61e58d]">{verified}</p></div><div className="rounded-2xl border border-[#244332] bg-[#0d1d13] p-5"><p className="text-xs text-[#9fb3a8]">Not verified</p><p className="mt-2 text-2xl font-bold text-[#f6c85f]">{rows.length-verified}</p></div></div><div className="mt-8 rounded-2xl border border-[#244332] bg-[#0d1d13] overflow-hidden"><div className="px-5 py-4 border-b border-[#244332]"><p className="font-semibold">Paystack verification status</p><p className="text-xs text-[#9fb3a8] mt-1">Legacy mock BVN entries cannot appear as verified here.</p></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-[#102719] text-[#9fb3a8]"><tr><th className="text-left px-5 py-3">User</th><th className="text-left px-5 py-3">Status</th><th className="text-left px-5 py-3">Verification</th><th className="text-left px-5 py-3">Paystack customer</th><th className="text-left px-5 py-3">Verified</th></tr></thead><tbody className="divide-y divide-[#244332]">{rows.map(({u,k,c,status,verified})=><tr key={u.id}><td className="px-5 py-4"><p className="font-semibold">{u.full_name||'Unnamed user'}</p><p className="text-xs text-[#9fb3a8] mt-0.5">{u.email||u.phone||'—'}</p></td><td className="px-5 py-4"><span className={'inline-flex rounded-full px-2.5 py-1 text-xs font-semibold '+(verified?'bg-[#123b23] text-[#61e58d]':'bg-[#3a2d10] text-[#f6c85f]')}>{pretty(status)}</span></td><td className="px-5 py-4 text-[#d6e1da]">{k?.verification_type||'—'}</td><td className="px-5 py-4 text-[#d6e1da]">{k?.provider_customer_ref||c?.provider_customer_code||c?.provider_customer_id||'—'}</td><td className="px-5 py-4 text-[#b7c7be]">{fmt(k?.verified_at||null)}</td></tr>)}</tbody></table></div></div></section>}