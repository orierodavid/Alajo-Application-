'use client'

import { useEffect, useState } from 'react'
export default function AdminNotificationsPage(){
 const[data,setData]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('')
 useEffect(()=>{fetch('/api/admin/finance',{cache:'no-store'}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to load notifications');setData(d.notifications??[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[])
 return <section className="p-5 sm:p-8 max-w-6xl mx-auto"><p className="text-xs font-bold tracking-[.2em] text-[#16a34a]">COMMUNICATIONS</p><h1 className="text-3xl font-bold mt-2">Notifications</h1><p className="text-gray-500 dark:text-gray-400 mt-2">Live system, KYC, contribution, payout and payment notifications.</p><div className="mt-8 rounded-2xl border border-gray-200 bg-white overflow-hidden"><div className="p-5 border-b border-gray-100"><p className="font-semibold">Notification activity</p></div>{loading?<p className="p-8 text-sm text-gray-500">Loading notifications…</p>:error?<p className="p-8 text-sm text-red-600">{error}</p>:data.length===0?<p className="p-8 text-sm text-gray-500">No notifications recorded yet.</p>:<div className="divide-y divide-gray-100">{data.map(n=><article key={n.id} className="p-5 flex items-start justify-between gap-5"><div><p className="font-semibold text-sm">{n.title}</p><p className="text-sm text-gray-500 mt-1">{n.body}</p><p className="text-xs text-gray-400 mt-2">{n.user?.full_name||n.user?.email||n.user_id} · {n.type}</p></div><span className="text-xs text-gray-400 whitespace-nowrap">{new Date(n.created_at).toLocaleString('en-NG')}</span></article>)}</div>}</div></section>
}
