'use client'

import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import { createClient } from '@/lib/supabase/client'

export default function AdminLoginPage() { const router=useRouter(); const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState(''); async function submit(event:FormEvent){event.preventDefault();setError('');setLoading(true);try{const supabase=createClient();const {data,error:signInError}=await supabase.auth.signInWithPassword({email:email.trim().toLowerCase(),password});if(signInError||!data.user){const message=signInError?.message?.toLowerCase()||'';if(signInError?.status===429||message.includes('rate limit'))setError('Too many login attempts. Please wait a moment and try again.');else setError('Invalid administrator credentials.');return}const {data:role,error:roleError}=await supabase.rpc('get_my_admin_role');if(roleError||!role){await supabase.auth.signOut();setError('This account does not have administrator access.');return}router.replace('/admin');router.refresh()}catch(error){console.error('[admin/login] authentication failure',error);setError('Unable to log in right now. Please try again.')}finally{setLoading(false)}} return <main className="min-h-screen bg-[#f5faf7] dark:bg-[#07150c] flex items-center justify-center px-5 py-10"><section className="w-full max-w-md"><div className="text-center mb-8"><div className="mx-auto w-12 h-12 rounded-2xl bg-[#16a34a] text-white flex items-center justify-center font-black text-xl">Z</div><p className="mt-4 text-xs font-bold tracking-[0.22em] text-[#16a34a]">ZEEPAY ADMINISTRATION</p><h1 className="mt-2 text-3xl font-bold text-gray-950 dark:text-white">Administrator Login</h1><p className="mt-2 text-sm text-gray-500 dark:text-gray-400">Secure access to the ZeePay operations backend.</p></div><form onSubmit={submit} className="rounded-3xl border border-gray-200 dark:border-white/15 bg-white dark:bg-[#102719] p-6 sm:p-8 shadow-xl"><label className="block text-sm font-semibold text-gray-800 dark:text-gray-200">Email address<input required type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-gray-200 dark:border-white/15 bg-white dark:bg-[#0b2113] text-gray-900 dark:text-white px-4 py-3 outline-none focus:ring-2 focus:ring-[#16a34a]/30"/></label><label className="block text-sm font-semibold text-gray-800 dark:text-gray-200 mt-5">Password<div className="mt-2 flex rounded-xl border border-gray-200 dark:border-white/15 bg-white dark:bg-[#0b2113]"><input required type={show?'text':'password'} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} className="min-w-0 flex-1 bg-transparent text-gray-900 dark:text-white px-4 py-3 outline-none"/><button type="button" onClick={()=>setShow(v=>!v)} className="px-4 text-sm text-gray-500">{show?'Hide':'Show'}</button></div></label>{error&&<div className="mt-5 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<button disabled={loading} className="mt-6 w-full rounded-xl bg-[#14532d] hover:bg-[#166534] disabled:opacity-60 text-white py-3.5 font-bold">{loading?'Signing in…':'Sign in to Admin'}</button></form><p className="text-center text-xs text-gray-400 mt-5">ZeePay administrative access is restricted to authorized staff.</p></section></main> }
