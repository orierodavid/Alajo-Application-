'use client'
import {useEffect,useState}from'react'
type S={key:string;numeric_value:number|null;boolean_value:boolean|null;integer_value:number|null}
type P={id:string;provider_id:string;provider_key:string;provider_name:string;provider_type:string;capability?:string;is_active:boolean;status:string;priority:number;credential_configured:boolean}
type M={id:string;country_code:string;country_name:string;currency_code:string;status:string;providers:P[]}
type Provider={id:string;provider_key:string;provider_type:string;display_name:string;status:string;credential_configured:boolean;markets:any[]}
const defaults:Record<string,number|boolean>={service_fee_percentage:0,delay_fee_percentage:0,auto_debit_enabled:true,reminder_before_days:3,reminder_after_days:1,reminder_repeat_days:2,max_reminders:3,auto_payout_enabled:true}
export default function AdminSettingsPage(){const[v,setV]=useState(defaults),[markets,setMarkets]=useState<M[]>([]),[providers,setProviders]=useState<Provider[]>([]),[role,setRole]=useState(''),[saving,setSaving]=useState(false),[rotating,setRotating]=useState(''),[keys,setKeys]=useState<Record<string,string>>({}),[msg,setMsg]=useState(''),[err,setErr]=useState('');useEffect(()=>{fetch('/api/admin/settings',{cache:'no-store'}).then(r=>r.json()).then(d=>{if(d.error)throw Error(d.error);const n={...defaults};for(const s of(d.settings||[])as S[])n[s.key]=s.boolean_value??s.integer_value??Number(s.numeric_value??0);setV(n)}).catch(e=>setErr(e.message));fetch('/api/admin/payment-providers',{cache:'no-store'}).then(r=>r.json()).then(d=>{if(d.error)throw Error(d.error);setRole(d.role||'');setMarkets(d.markets||[]);setProviders(d.providers||[]) }).catch(e=>setErr(e.message))},[]);const set=(k:string,x:number|boolean)=>setV(a=>({...a,[k]:x}));async function save(){setSaving(true);setMsg('');try{const r=await fetch('/api/admin/settings',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(v)}),d=await r.json();if(!r.ok)throw Error(d.error);setMsg('Settings saved successfully.')}catch(e){setErr(e instanceof Error?e.message:'Unable to save settings.')}finally{setSaving(false)}}async function provider(id:string,on:boolean){setErr('');const r=await fetch('/api/admin/payment-providers',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({configId:id,isActive:on})}),d=await r.json();if(!r.ok){setErr(d.error||'Unable to update provider.');return}setMarkets(ms=>ms.map(m=>({...m,providers:m.providers.map(p=>p.id===id?{...p,is_active:on,status:on?'ACTIVE':'DISABLED'}:p)})));setMsg('Provider configuration updated.')}async function rotate(p:Provider){const secret=(keys[p.id]||'').trim();if(!secret){setErr('Enter an API key before rotating.');return}setRotating(p.id);setErr('');setMsg('');try{const r=await fetch('/api/admin/payment-providers',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'rotate_credential',providerId:p.id,secret})}),d=await r.json();if(!r.ok)throw Error(d.error);setProviders(ps=>ps.map(x=>x.id===p.id?{...x,credential_configured:true}:x));setKeys(k=>({...k,[p.id]:''}));setMsg(`${p.display_name} credential saved securely.`)}catch(e){setErr(e instanceof Error?e.message:'Unable to save credential.')}finally{setRotating('')}}
const Panel=({t,e,children}:{t:string;e:string;children:React.ReactNode})=><section className="rounded-3xl border border-white/10 bg-[#0b2113] p-5 sm:p-6"><p className="text-[10px] uppercase tracking-[.2em] text-[#4ade80] font-bold">{e}</p><h2 className="mt-1 text-base font-bold text-white">{t}</h2><div className="mt-4">{children}</div></section>;const Row=({l,d,children}:{l:string;d:string;children:React.ReactNode})=><div className="flex items-center justify-between gap-4 py-4 border-b border-white/10 last:border-0"><div><p className="text-sm font-semibold text-white">{l}</p><p className="text-[11px] text-gray-300 mt-1">{d}</p></div>{children}</div>;const Num=({l,d,k,s=''}:{l:string;d:string;k:string;s?:string})=><Row l={l} d={d}><div className="flex items-center gap-1"><input type="number" min="0" step="0.01" value={Number(v[k])} onChange={e=>set(k,Number(e.target.value))} className="h-9 w-20 rounded-xl border border-white/15 bg-white/5 px-2 text-right text-sm text-white"/><span className="text-xs text-gray-300">{s}</span></div></Row>;const Toggle=({l,d,k}:{l:string;d:string;k:string})=><Row l={l} d={d}><button onClick={()=>set(k,!v[k])} className={`h-7 w-12 rounded-full border p-0.5 ${v[k]?'bg-[#16a34a] border-[#4ade80]':'bg-white/10 border-white/20'}`}><span className={`block h-6 w-6 rounded-full bg-white transition-transform ${v[k]?'translate-x-5':''}`}/></button></Row>;
return <main className="admin-settings min-h-screen px-5 py-7 sm:px-8 bg-[#06130b] text-white"><div className="max-w-[1280px] mx-auto"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] uppercase tracking-[.22em] font-bold text-[#4ade80]">System control center</p><h1 className="text-3xl sm:text-4xl font-black mt-1 text-white">Settings</h1><p className="text-sm text-gray-300 mt-2">Configure fees, automation, country routing and protected provider credentials.</p></div><button onClick={save} disabled={saving} className="rounded-xl bg-[#16a34a] px-5 py-2.5 text-sm font-bold text-white disabled:opacity-50">{saving?'Saving…':'✓ Save changes'}</button></header>{err&&<div className="mt-4 rounded-xl border border-red-400/40 bg-red-500/10 p-3 text-red-100">{err}</div>}{msg&&<div className="mt-4 rounded-xl border border-[#4ade80]/30 bg-[#16a34a]/10 p-3 text-[#bbf7d0]">{msg}</div>}<div className="mt-5 grid gap-5 xl:grid-cols-3"><Panel e="01 / Finance" t="Fees"><Num l="Service fee" d="First successful contribution." k="service_fee_percentage" s="%"/><Num l="Delay fee" d="Applied after the 29th." k="delay_fee_percentage" s="%"/><Toggle l="Wallet auto-debit" d="Attempt contribution from wallet." k="auto_debit_enabled"/></Panel><Panel e="02 / Engagement" t="Reminders"><Num l="Before due date" d="Days before the 1st." k="reminder_before_days" s="days"/><Num l="After missed payment" d="Days after deadline." k="reminder_after_days" s="days"/><Num l="Repeat interval" d="Reminder frequency." k="reminder_repeat_days" s="days"/><Num l="Maximum reminders" d="Maximum per contribution." k="max_reminders"/></Panel><Panel e="03 / Transfers" t="Payout automation"><Toggle l="Automatic payouts" d="Process eligible payouts." k="auto_payout_enabled"/><Row l="Contribution window" d="Monthly contribution period."><b className="text-white">1st–29th</b></Row></Panel></div>
<section className="mt-5 rounded-3xl border border-[#1c5130] bg-[#081b10] p-5 sm:p-6"><div className="flex flex-col sm:flex-row sm:items-end justify-between gap-3"><div><p className="text-[10px] uppercase tracking-[.2em] text-[#4ade80] font-bold">04 / Provider credentials</p><h2 className="text-xl font-black text-white mt-1">API key management</h2><p className="text-sm text-gray-300 mt-1">Credentials are encrypted in Supabase Vault. Existing keys are never returned to the browser.</p></div>{role==='super_admin'?<span className="rounded-full border border-[#4ade80]/25 bg-[#16a34a]/10 px-3 py-1 text-xs font-bold text-[#bbf7d0]">Super Admin</span>:<span className="rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs font-bold text-gray-300">Read-only credentials</span>}</div><div className="mt-5 grid md:grid-cols-2 gap-3">{providers.map(p=><div key={p.id} className="rounded-2xl border border-white/10 bg-white/[.035] p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold text-white">{p.display_name}</h3><p className="text-[10px] text-gray-400 mt-1">{p.provider_type} · {p.provider_key}</p></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-black ${p.credential_configured?'bg-[#16a34a]/20 text-[#86efac]':'bg-amber-500/10 text-amber-200'}`}>{p.credential_configured?'CONFIGURED':'NOT CONFIGURED'}</span></div>{role==='super_admin'?<div className="mt-4 flex gap-2"><input type="password" autoComplete="new-password" value={keys[p.id]||''} onChange={e=>setKeys(k=>({...k,[p.id]:e.target.value}))} placeholder={p.credential_configured?'Enter new key to rotate':'Enter API key'} className="min-w-0 flex-1 h-10 rounded-xl border border-white/15 bg-white/5 px-3 text-sm text-white placeholder:text-gray-500"/><button onClick={()=>rotate(p)} disabled={rotating===p.id} className="rounded-xl bg-[#16a34a] px-4 text-xs font-black text-white disabled:opacity-50">{rotating===p.id?'Saving…':p.credential_configured?'Rotate':'Save'}</button></div>:<p className="mt-4 text-xs text-gray-300">Credential status is visible. API keys can only be added or rotated by the Super Admin.</p>}</div>)}</div></section>
<section className="mt-5 rounded-3xl border border-[#1c5130] bg-[#081b10] p-5 sm:p-6"><div className="flex flex-col sm:flex-row sm:items-end justify-between gap-3"><div><p className="text-[10px] uppercase tracking-[.2em] text-[#4ade80] font-bold">05 / Country routing</p><h2 className="text-xl font-black text-white mt-1">Payment API by country</h2><p className="text-sm text-gray-300 mt-1">Turn configured providers on or off per country.</p></div><span className="rounded-full border border-[#4ade80]/25 bg-[#16a34a]/10 px-3 py-1 text-xs font-bold text-[#bbf7d0]">{markets.length} markets</span></div><div className="mt-5 grid md:grid-cols-2 xl:grid-cols-3 gap-3">{markets.map(m=><div key={m.id} className="rounded-2xl border border-white/10 bg-white/[.035] p-4"><div className="flex justify-between gap-3"><div><h3 className="font-bold text-white">{m.country_name}</h3><p className="text-[11px] text-gray-400">{m.country_code} · {m.currency_code}</p></div><span className="text-[10px] font-bold text-[#86efac]">{m.status}</span></div><div className="mt-4 space-y-2">{m.providers.length?m.providers.map(p=><div key={p.id} className="flex items-center justify-between rounded-xl border border-white/10 px-3 py-2.5"><div><p className="text-xs font-bold text-white">{p.provider_name}</p><p className="text-[10px] text-gray-400">{p.provider_type} · {p.environment} · priority {p.priority}</p></div><button onClick={()=>provider(p.id,!p.is_active)} className={`rounded-lg px-3 py-1.5 text-[10px] font-black ${p.is_active?'bg-[#16a34a] text-white':'bg-white/10 text-gray-300'}`}>{p.is_active?'ACTIVE':'ENABLE'}</button></div>):<p className="text-xs text-gray-400 py-3">No provider configured.</p>}</div></div>)}</div></section></div></main>}
