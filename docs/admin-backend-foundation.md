# Alajo Admin Backend Foundation

The admin backend now has a dedicated login, role-gated middleware, operations dashboard, unified navigation, and reserved operational areas.

Admin access is controlled by `public.user_roles`; no admin credentials are stored in the application repository.
